# JeevanPath AI — Backend Requirements
#
# ============================================================================
# IMPORTANT: PyTorch is NOT listed here — it is already installed with CUDA
# support (torch==2.11.0+cu128).  Installing from this file would replace
# the CUDA build with a CPU-only version from standard PyPI.
#
# DO NOT run:  pip install torch   ← breaks CUDA installation
#
# Install these requirements with:
#   pip install -r requirements.txt
# ============================================================================

# --- Web framework ---
fastapi>=0.139.0           # FastAPI (Pydantic v2 compatible)
uvicorn[standard]>=0.51.0  # ASGI server with WebSocket support

# --- Config & validation ---
pydantic>=2.13.0           # Data validation (Pydantic v2)
pydantic-settings>=2.15.0  # .env-based settings on top of Pydantic

# --- Request parsing ---
python-multipart>=0.0.32   # Required for FastAPI file upload (UploadFile)

# --- Structured logging ---
structlog>=26.0.0           # JSON + dev-console structured logging

# --- Monitoring ---
prometheus-client>=0.26.0  # Prometheus metrics

# --- Speech recognition ---
faster-whisper>=1.2.0      # CTranslate2-based Whisper: fast + low VRAM

# --- Database ---
sqlalchemy[asyncio]>=2.0.0  # Async ORM
alembic>=1.19.0             # Schema migrations
asyncpg>=0.30.0             # Async PostgreSQL driver (for SQLAlchemy async)
psycopg2-binary>=2.9.9      # Sync PostgreSQL driver (for Alembic)
pgvector>=0.3.0             # pgvector Python client (VECTOR type support)

# --- AI / ML (install when ready — see notes below) ---
# sentence-transformers>=3.0.0   # Embedding model (indic-sentence-bert)
# transformers>=4.40.0            # HuggingFace Transformers (LLM + MMS-TTS)
# accelerate>=0.30.0              # Model loading on GPU (required by transformers)
# bitsandbytes>=0.43.0            # 4-bit / 8-bit quantisation (optional)
# numpy>=1.24.0                   # Required by MMS-TTS WAV writer
#
# Install all AI deps in one go when you're ready to wire real models:
#   pip install sentence-transformers transformers accelerate numpy

# --- Dev / test ---
# pytest>=8.0.0
# pytest-asyncio>=0.23.0
# httpx>=0.27.0                   # Async HTTP client for FastAPI TestClient
